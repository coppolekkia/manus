services:
  openmanus:
    build: .
    container_name: openmanus_agent
    volumes:
      # This mounts the config folder so you can inject your API keys easily
      - ./config:/app/config
    restart: unless-stopped
